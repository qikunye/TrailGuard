// S2 — Camera / file upload for incident evidence
// Props: { onUpload(file) }
export default function PhotoUpload({ onUpload }) {
  return (
    <div className="section-card">
      <p className="page-subheading">PhotoUpload — S2</p>
    </div>
  );
}

// S2 — Shows ETA + list of notified emergency contacts
// Props: { eta: string, contacts: string[] }
export default function EmergencyStatusCard({ eta, contacts = [] }) {
  return (
    <div className="section-card">
      <p className="page-subheading">EmergencyStatusCard — S2</p>
    </div>
  );
}

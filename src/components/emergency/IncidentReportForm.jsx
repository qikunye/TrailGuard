// S2 — Main injury report form
// Props: { onSubmit(formData) }
export default function IncidentReportForm({ onSubmit }) {
  return (
    <div className="section-card">
      <p className="page-subheading">IncidentReportForm — S2</p>
    </div>
  );
}

// S1 — Trail search input; calls onSearch(query) prop
export default function TrailSearchForm({ onSearch }) {
  return (
    <div className="section-card">
      <p className="page-subheading">TrailSearchForm — S1</p>
    </div>
  );
}

// S1 — Displays current weather for selected trail
// Props: { weather: { temp, condition, wind, humidity } }
export default function WeatherWidget({ weather }) {
  return (
    <div className="section-card">
      <p className="page-subheading">WeatherWidget — S1</p>
    </div>
  );
}

// S1 — Compares available daylight vs estimated trail completion time
// Props: { daylightMinutes: number, requiredMinutes: number }
export default function CompletionFeasibility({ daylightMinutes, requiredMinutes }) {
  return (
    <div className="section-card">
      <p className="page-subheading">CompletionFeasibility — S1</p>
    </div>
  );
}

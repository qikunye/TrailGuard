// S3 — Displays an alternative trail option
// Props: { route: { name, distance, estimatedTime, difficulty } }
export default function AlternativeRouteCard({ route }) {
  return (
    <div className="section-card">
      <p className="page-subheading">AlternativeRouteCard — S3</p>
    </div>
  );
}

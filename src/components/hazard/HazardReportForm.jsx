// S3 — Hazard report form (type + description + location)
// Props: { onSubmit(formData) }
export default function HazardReportForm({ onSubmit }) {
  return (
    <div className="section-card">
      <p className="page-subheading">HazardReportForm — S3</p>
    </div>
  );
}
